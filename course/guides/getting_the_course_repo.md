# Getting the Course Repository

> ⛔ **DO NOT EDIT THIS FILE.** It is replaced whenever you run `git pull course main`.
> Your own work goes in `my_work/`. To use a template, copy it there first.
> *If a pull ever fails with "local changes would be overwritten", you edited something in here.*

**ARTD8105 · v1.0 · 26 August 2026**
*Session 2, first thirty minutes. Once, ever. After this it is one command.*

---

## What you are doing, and why it is not a clone

You have your own repository — the one you made in the session 1 homework, named `artd8105-<yoursurname>`. I have mine, the one you are reading now. **You need both sets of files in one folder**, so that the agent can see the course material and your work at the same time.

The obvious move is to clone my repository inside yours. **Do not do that.** A git repository inside another git repository confuses both of them: your commits stop covering the inner folder, GitHub shows an empty grey box where the files should be, and you will lose an hour to it.

What you do instead is add my repository as a **second source** for your existing one:

```
your repository  ──  origin   →  github.com/you/artd8105-yoursurname   (you push here)
                 └─  course   →  github.com/AtticusSims/ARTD8105        (you only pull)
```

One folder. One repository. Two places it talks to. My files land alongside yours, `my_work/` stays entirely yours, and when I publish something new you type one command and have it.

---

## Before you start — the thirty-second check

Open Cursor on your project folder, open the chat, and paste this. **It only looks. It changes nothing.**

```
Check my ARTD8105 setup and report what you find. Do not change anything yet.

1. What folder am I in, and is it a git repository?
2. What remotes exist, and what URL does each point to?
3. Is there anything uncommitted right now?
4. Does a folder called course/ or my_work/ already exist here?

Answer in plain language, then tell me which of these describes me:
  A — I have a git repository with my work in it, and no `course` remote
  B — I have a folder with my work in it, but it is not a git repository
  C — the folder is empty, I am starting from nothing
```

Then go to the matching section below. **If the agent says you already have a `course` remote, you are done** — skip to *From now on*.

---

## Path A — you have a repository already

This is where you should be if the session 1 homework went as planned.

**Commit whatever you have first.** A pull on top of uncommitted changes is the single most common way to make a mess. Paste this:

```
Before we bring in the course files: commit everything I currently have,
with a sensible message, and push it to origin. Then confirm the working
tree is clean.
```

**Then bring in the course repository:**

```
Add the ARTD8105 course repository as a second remote called `course` and
pull it in, so that the course files sit alongside my own work in this same
repository. It is at:

    https://github.com/AtticusSims/ARTD8105.git

Our two repositories have no shared history, so the pull needs
--allow-unrelated-histories. Do not clone it into a subfolder — that would
nest one repository inside another, which I do not want.

Show me each command before you run it and tell me what it does. If
anything fails, stop and explain the error rather than trying something else.
```

**What the agent should run.** Read these, because in November you will be asked what your own repository does:

```bash
git remote add course https://github.com/AtticusSims/ARTD8105.git
git fetch course
git pull course main --allow-unrelated-histories
```

`remote add` saves the address under the nickname `course`. `fetch` downloads it without changing your files. `pull` merges it into your folder. The `--allow-unrelated-histories` flag is git refusing, sensibly, to combine two histories that never met — until you tell it that is exactly what you meant.

---

## Path B — you have work, but it is not a repository

Your files exist; git was never set up. Twenty minutes. Do this now, then Path A.

Work through **`git_and_github_setup.md`** properly at some point this week — it explains why each step exists. To get running now, paste this:

```
Set this folder up as a git repository for ARTD8105:

1. Initialise git here and set the default branch to main.
2. Create a .gitignore that excludes PDFs, video, raw camera files,
   .blend files, and a _local/ folder.
3. Commit everything currently here.

Then stop and tell me what to do on GitHub. Do not create anything online —
I will do that part myself in the browser.
```

Then, in your browser: **github.com → + → New repository**, named `artd8105-<yoursurname>`, **Private**, and **do not** tick "add a README". Copy the URL it gives you, come back, and paste:

```
My GitHub repository is at <paste your URL here>.

Add it as `origin`, push main, and set the upstream so that plain `git push`
works from now on. If GitHub asks me to sign in, tell me what is happening
before it opens the browser.
```

Then do **Path A**.

> **Do not forget the collaborator step.** Repository → Settings → Collaborators → Add people → my GitHub username → **Read** access. Nobody remembers this and it is invisible until the day it matters.

---

## Path C — you are starting from nothing

You joined after session 1, or the setup did not happen. You can be running inside half an hour, and you will not be behind by the end of today.

**1 · Install Cursor.** From **cursor.com**. Sign in. A subscription is required for the term — if cost is a problem, email me this week rather than going quiet.

**2 · Install git.**

- **Windows** — [git-scm.com/download/win](https://git-scm.com/download/win), accept every default.
- **macOS** — open Terminal, type `git --version`, press enter. If it is missing, macOS offers to install it.

**3 · Tell git who you are.** Once, ever. In Cursor's terminal:

```bash
git config --global user.name "Your Name"
git config --global user.email "your.email@um.edu.mo"
```

Use your UM email. It is what links your commits to you, and your commit history is assessed evidence in this course.

**4 · Make a folder** somewhere sensible — `Documents/ARTD8105` is fine. Open Cursor on it: **File → Open Folder**.

**5 · Now do Path B**, then **Path A**.

**6 · You also need an instruction.** Everyone else wrote one by hand last week and had two people execute it. You have not, and today's work builds directly on it. Write one now — ten minutes, in `my_work/instruction.md`:

> A procedure another person could carry out with pencil and paper. **Executable** — every step is an action, not a feeling. **Variable** — two people following it should produce recognisably different drawings. **Bounded** — it ends.
>
> Then, before you show anyone: write down in one line what you expect it to produce. That line is the baseline, and without it nothing you see later can honestly be called a surprise.

You will not have the two hand executions the others have, and that genuinely is a loss — the disagreement between two people is the clearest evidence of where a decision is hiding. Catch up cheaply: hand your instruction to someone at the next table during the break and have them draw it. Ten minutes buys you most of what you missed.

---

## What it looks like when it worked

Ask the agent:

```
Confirm the setup is correct: list my remotes with their URLs, show me the
top level of this folder, and tell me whether my_work/ and course/ are both
here. One short answer.
```

You should have both remotes — `origin` pointing at your repository, `course` at mine — and a folder that looks like this:

```
artd8105-yoursurname/
├── .cursor/rules/     how the agent behaves — loads automatically
├── context/           rubric, logging spec, glossary
├── course/            guides, templates, session briefs, examples
├── my_work/           ★ everything you make goes here
└── README.md
```

**Open `my_work/README.md` and read it.** It is short, and it is the map of where each thing you make this term is supposed to land.

---

## From now on

Two commands, for the rest of the term:

```bash
git pull course main     # get new material from me
git push                 # send your work to your own repository
```

Or in the chat, which is what most of you will actually do:

> *"Pull the latest course material."*
> *"Commit what I did today and push it."*

**Commit at the end of every working session, not at the end of every week.** A commit is a timestamped, content-hashed record of what existed when — which is why the rubric can assess your process at all. Write the messages for the person who reads them back in session 10, because that person is you:

```
S2: parameters.md - 9 ambiguities named, 3 still undecided
S2: tool.html builds, angle spread behaves backwards from what I predicted
```

Not `update`. Not `stuff`.

---

## When it goes wrong

**`fatal: not a git repository`** — you are in the wrong folder. In Cursor, **File → Open Folder** and pick your project folder, not its parent and not your desktop.

**`refusing to merge unrelated histories`** — the `--allow-unrelated-histories` flag was left off. Run the pull again with it.

**`remote course already exists`** — you already did this, possibly in a previous attempt. Harmless. Ask the agent to show you where `course` points and to correct the URL if it is wrong.

**`Your local changes to the following files would be overwritten by merge`** — you edited a file outside `my_work/`. Everything in `course/`, `context/` and `.cursor/` is replaced on every pull, which is why they carry that warning at the top. Nothing is lost; ask the agent:

> *"I edited a file outside my_work/ and now the pull is blocked. Show me exactly what I changed, then help me move it into my_work/ and restore the original."*

**Authentication fails, or GitHub asks repeatedly** — use the browser sign-in Cursor offers rather than typing a password; GitHub stopped accepting account passwords over git years ago. If it loops, check two-factor authentication is actually finished on your account.

**A push is rejected as too large** — you committed a PDF, a video or a camera raw file. Do not try to rewrite history yourself:

> *"A push is failing because of a large file. Show me which file and how big, then walk me through removing it from history one command at a time, explaining each before I run it."*

**Anything else** — the agent has your terminal and can read the actual error, which is more than I can do from the front of the room. Paste the error in and ask what it means. If you are still stuck after ten minutes, ask me in class or email me. Arriving stuck costs you a session; asking costs you nothing.

---

## Last resort — no git at all

If git is genuinely broken on your machine and class is happening around you:

1. Go to **github.com/AtticusSims/ARTD8105**
2. Green **Code** button → **Download ZIP**
3. Unzip it and copy `course/`, `context/`, `.cursor/` and `README.md` into your project folder

You will have today's files and you can work. **You will not get updates**, so this is a way to survive the session, not a way to run the term. Fix git properly before session 3 — bring the machine to me if you need to.
